// Adresse /exec du déploiement Apps Script (voir apps-script/INSTALL.md).
// Laissée vide, l'app la demande avec le PIN et la garde sur l'appareil
// (pv_url). Elle n'est pas secrète : sans PIN, le script refuse tout.
export const SCRIPT_URL = '';
